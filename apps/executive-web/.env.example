# Server-only — never use NEXT_PUBLIC_ for these secrets.
EXECUTIVE_MCP_BASE_URL=https://executive-mcp.example.com
EXECUTIVE_API_KEY=replace-me
# Force fixtures even when BASE_URL is set (local UI work).
# EXECUTIVE_USE_FIXTURES=true
